export enum MemberType {  // membertype un enum
    USER = 'USER',
    RESTAURANT = "LIBRARY",
}

export enum MemberStatus {
    ACTIVE = "ACTIVE",
    BLOCK = "BLOCK",
    DELETE = "DELETE",
}